# python-scaffold

Basic scaffold project for testing python.  Uses:

- pipenv
- unittest
- hypothesis
- mypy

To initialize:

- `pipenv sync`

To REPL:

- `pipenv shell`
- `python`
- `import scaffold`

To test:

- `./verify.sh` (Runs typelint and then unittests)
